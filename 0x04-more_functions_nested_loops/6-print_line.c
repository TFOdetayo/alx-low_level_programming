#include "main.h"

/**
 * print_line - a function that draws a straight li
 * @n: the argument.
 * Return: 0.
 */

void print_line(int n)
{
	int a;

	for (a = 1; a <= n; a++)
	{
		_putchar('_');
	}
	if (a <= 0)
	{
		_putchar(10);
	}
	else
	{
		_putchar(10);
	}
}
